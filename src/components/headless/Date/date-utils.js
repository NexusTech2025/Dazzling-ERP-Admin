/**
 * Constant configuration properties governing static calendar date transformations.
 */
export const DATE_DEFAULTS = {
  INPUT_FORMAT: undefined, // Defaults parsing strategy to automated ISO/Fallback detection
  OUTPUT_FORMAT: 'd, MMMM yyyy', // Example: "16, July 2026"
  FALLBACK: '--'
};
